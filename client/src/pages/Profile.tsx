import { useEffect } from 'react';
import Navbar from '../components/Navbar';

export default function ProfilePage() {

	useEffect(() => {
	}, []);

	return (
		<>
			<Navbar />
			<h1>Profile</h1>
		</>
	);
}